import React, { FC } from 'react'
import styled from 'styled-components'

const IsWinContainer = styled.div``

export const IsWin: FC = (): JSX.Element => {
  return <IsWinContainer>Win!</IsWinContainer>
}
