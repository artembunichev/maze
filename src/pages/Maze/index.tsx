import React, { FC } from 'react'
import styled from 'styled-components'

const MazePageContainer = styled.div``

export const MazePage: FC = (): JSX.Element => {
  return <MazePageContainer>MAZE</MazePageContainer>
}
